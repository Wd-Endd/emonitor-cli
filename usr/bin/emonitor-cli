#!/usr/bin/env bash

cd "$(dirname $0)"
LIB="./../lib/emonitor-cli"

# if [ $1='' ]; then
#     echo "error"
# fi

while getopts "rsc" opt; do
    case "$opt" in
        r) $LIB/ram;;
        s) $LIB/swap;;
        c) $LIB/cpu;;
    esac
done